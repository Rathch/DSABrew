{
  "name": "dsabrew-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "prepare-assets": "node scripts/export-banner.mjs && node scripts/copy-parchment-assets.mjs && node scripts/slice-gm-frame.mjs",
    "import-gm-frame": "node scripts/import-gm-meister-frame.mjs",
    "predev": "npm run prepare-assets",
    "prebuild": "npm run prepare-assets",
    "postinstall": "npm run prepare-assets"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^2.5.2",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "typescript": "^5.9.2",
    "vite": "^5.4.11",
    "vitest": "^3.2.4"
  }
}
